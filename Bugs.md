- FPS regression (reported 15-20 FPS): measured improvement after limiting repeated missing-name fallback searches to once per synchronous physics update. Across the final 30 profiling windows before/after, average main PhysX time fell from 2.594 to 1.579 ms/frame (39%); FindObjC calls fell from 4,136 to 1,795/frame, and axis lookup time from 1.228 to 0.569 ms/frame. Recorded frame rate rose from about 66 to 84 FPS, but scene/contact activity differed, so this is not a controlled FPS benchmark or proof of complete recovery. Awaiting the user's assessment of remaining FPS loss. Direct lookups and successful binding validation remain live; misses expire at update boundaries and invalidate on names, cloning, imports, generation or script-engine changes. Native commands end reuse immediately. Lookup, pose, gravity/camera, Customizer, native-command and lifecycle regression tests passed. Baseline and comparison logs: build/performance-engine-before-optimization.log and build/performance-after-lookup-optimization.log. Profiling disabled again after capture.

- <s>Wind carried over through the main menu into a room without wind.</s> Fixed; user reports the latest build seems fine. Game_Abort, NewGame_Start and Exit clear room wind and retire room-chain bindings before native scene disposal. Late outgoing scene observations are blocked until the next room starts. Lifecycle and native-command regressions pass.
- <s>Freeze/crash when quitting.</s> User reports the latest build seems fine; the inspected session recorded the shutdown guard and an empty fault log. Exit now stops PhysX callbacks before native teardown, and room disposal forgets stale bindings without writing through them. The earlier fault occurred during WComboBox destruction; stale room writes were a plausible cause, not proven by the trace alone. Shutdown and lifecycle regressions pass.

- <s>Customizer retains displaced body physics after entering from a room.</s> Fixed; confirmed in game. Hidden room actors no longer contribute cross-person body collisions in Customizer. Own-body contacts and normal room collisions remain active.
- <s>PoseEditor OFF-to-ON snaps penis joints02/03 instead of preserving their bends.</s> Fixed; confirmed in game. Evaluated native track bends are captured before neutralization and eased out through the native property setter. Keyframes and OFF restoration are preserved.
- Testicle smooth activation: implemented; specific in-game confirmation still pending. Evaluated testicles_joint01/02 bends seed the existing smooth-start springs. The end joint stays unsimulated. Regression tests cover initial output, restoration, reset and empty-track fallback.
- <s>Customizer black-screen duration.</s> Fixed; confirmed in game. Verified inactive actors no longer hold the fade behind the timeout. The visible actor still needs confirmed gravity and two ready frames. Unknown bindings remain guarded.

- <s>Breast gravity_inward_strength and gravity_outward_strength use the root instead of spine_joint04.</s> Fixed.
- PhysX Settings is missing from the right-click menu in PoseEditor's advanced preview. Deferred; the earlier investigation judged the required engine changes disproportionate to this UI issue. Not fixed.
- <s>PoseEditor testicle track controls remain enabled while testicle PhysX is on.</s> Fixed.
- <s>CcConfigEditorPhysX spinboxes do not synchronize with Config.ini.</s> Resolved in the existing bug list.
- <s>A blank PoseEditor pose inherits the previous pose's animation; OFF rotation or physics axes are wrong.</s> Fixed; user confirmed correct default rotation and physics axes. Native command decoding and deferred pose-reset hooks return track ownership before replacement and prevent simulation from overwriting incoming values. The read-only loading observer remains active to capture the correct orientation references. Native-command and pose-transition regressions cover repeated loads, OFF restoration, replacement skeletons and camera cancellation.
- Slight jump during New/Load with PhysX enabled: the restarted solver now starts from the incoming pose's output with no previous-pose motion. Later native-fade changes were confirmed seamless; a separate verdict on any residual solver jump was not recorded.
- Persistent breast displacement during pose changes: cleanup now restores breast/butt rotation and translation before discarding their rest snapshots, only for the matching live skeleton. Production regressions pass across repeated resets, all four people and replacement skeletons. Specific in-game confirmation remains pending.
- <s>Body physics visibly resets before the new pose appears.</s> Fixed; user confirmed seamless native fades. Cleanup waits until native fade readiness allows pose application. Native loading and rendering continue normally. Pose-transition and presentation regressions pass.

- <s>Body and add-on gravity waits for camera movement to finish.</s> Fixed for the confirmed body/add-on cases. Gravity follows clean body-relative orientation using a confirmed room reference. Replaced bones must reconfirm their reference. Later pose-change startup reports are tracked below.
- Possible minor camera contamination immediately after pose changes or during startup: reported but unconfirmed. Synthetic startup/replacement tests pass; retain for targeted in-game checking.
- <s>Collision startup waits for camera movement despite a valid room-placement frame.</s> Fixed; confirmed in game. Confirmed body-local colliders no longer wait for a quiet view-space root. Startup sample checks and guards for unconfirmed sources remain active.
- <s>Entering Customizer from PoseEditor or FreeMode delays physics by about 1-2 seconds.</s> Fixed; tested in game and approved. Verified live bindings and collision samples are refreshed promptly; unsupported sources retain guarded startup. Activation on the first visible frame is not guaranteed.
- Customizer round trip: entry now fades to black before native cleanup, and clean placement rotations can rebase confirmed gravity without waiting for the camera. The user confirmed the entry jump improved; the later retained-body and black-screen issues are resolved above. The specific room -> Customizer -> room -> pose-switch delay still needs explicit confirmation.
- <s>Body PhysX OFF-to-ON snaps immediately to its simulated pose.</s> Fixed; smooth activation confirmed in game. Simulation starts from the current pose with zero inherited velocity and settles through the existing springs. The later joint02/03 refinements are tracked above.
- Pose-change startup delay: confirmed placement/gravity references can survive a solver reset only while their bindings and placement properties match. Geometric chain gravity uses the confirmed primary direction instead of a second camera wait; translation-only placement changes retain orientation but discard old contact placement. Production camera, gravity and collision regressions pass. Initial in-game results improved, but the Customizer round-trip delay reported afterward has no explicit final confirmation.
